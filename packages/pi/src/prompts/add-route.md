---
name: add-route
description: "Add a new route to the handler. Usage: /template:add-route <METHOD> <path>"
---
Add a {{1}} {{2}} route to the current handler. Read the target file. Run `zts_expert_verify_paths`. Gather current module facts with `zts_expert_modules`. Author the COMPLETE file content yourself. Dry-run the draft with `zts_expert_edit_simulate` and resolve every new violation. Submit exactly one `apply_edit` call. The host compiler veto and approval policy own the write.
