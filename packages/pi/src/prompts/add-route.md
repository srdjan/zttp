---
name: add-route
description: "Add a new route to the handler. Usage: /template:add-route <METHOD> <path>"
---
Add a {{1}} {{2}} route to the current handler. Read the target file. Run `zts_expert_verify_paths`. Gather current module facts with `zts_expert_modules`. Author the COMPLETE file content yourself for every changed path and submit exactly one `propose_change_set` call. The host proves the aggregate draft before the approval policy can commit it.
